import Reveal from "./Reveal";export default function SectionHeading({label,title,sub,align="left"}:{label:string;title:string;sub?:string;align?:"left"|"center"}){return <div className={align==="center"?"text-center":""}><Reveal><p className="section-label mb-4">{label}</p></Reveal><Reveal delay={.08}><h2 className="font-display text-4xl md:text-5xl lg:text-6xl font-bold tracking-tightest leading-[1.02] whitespace-pre-line">{title}</h2></Reveal>{sub&&<Reveal delay={.16}><p className={`mt-5 text-mist max-w-xl text-base md:text-lg leading-relaxed ${align==="center"?"mx-auto":""}`}>{sub}</p></Reveal>}</div>}
